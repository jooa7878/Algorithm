#include <iostream>
using namespace std;

int factorial(int num) {
	int result = num;
	if (num == 1 || num == 0)  return 1; 
	else {
		result *= factorial(num - 1);
	}
	return result;

}

int main() {
	cin.tie(NULL);

	int N;

	cin >> N;

	cout << factorial(N) << '\n';

} 
